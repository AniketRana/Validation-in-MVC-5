@model LooselyBindValidation.Models.Emp

@{
    Layout = null;
}

<!DOCTYPE html>

<html>
<head>
    <meta name="viewport" content="width=device-width" />
    <title>Index</title>
</head>
<body>

    @*<div>
          <h4>Below are loosely binding validation </h4>
            @using (Html.BeginForm("SubmitData", "Home", FormMethod.Post))
            {
                @Html.TextBox("Fname") @Html.ValidationMessage("Fname")<br />
                @Html.TextBox("Lname") @Html.ValidationMessage("Lname")<br />
                @Html.TextBox("Email") @Html.ValidationMessage("Email")<br />
                @Html.Editor("Age") @Html.ValidationMessage("Age")<br /><br />
                <input type="submit" value="Save data" />
            }
        </div>*@

    <div>
        <h4>Below are strongly binding validation </h4>
        @using (Html.BeginForm("SubmitData", "Home", FormMethod.Post))
        {

            @Html.ValidationSummary() //For validation summary 


            @Html.TextBoxFor(x => x.Fname) <br />

            @Html.TextBoxFor(x => x.Lname) <br />
            
            @Html.TextBoxFor(x => x.Email) <br />
            
            @Html.EditorFor(x => x.Age) <br /><br />
            
            <input type="submit" value="Save data" />
        }
    </div>
</body>
</html>
